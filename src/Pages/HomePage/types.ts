export interface HomeResponse {
  characterUri: string;
  nickname: string;
  tierName: string;
  testResult: string;
}
export type PropensityResponse = {
  isTested: boolean;
  propensityKoreanName?: string;
};
